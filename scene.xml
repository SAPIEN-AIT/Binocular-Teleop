<mujoco model="binocular_teleop">
  <include file="leap_hand/right_hand.xml"/>

  <statistic center="0 0.3 0.3" extent="0.6"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="120" elevation="-20"/>
  </visual>

  <worldbody>
    <light directional="true" diffuse=".5 .5 .5" specular="0.1 0.1 0.1"
           pos="0 0 1.5" dir="0 0 -1"/>
    <geom name="floor" type="plane" size="1 1 1" rgba="0.2 0.3 0.4 1"/>

    <!-- Kinematic anchor: vision drives this body; weld constraint pulls the LEAP palm -->
    <body name="hand_proxy" mocap="true" pos="0 0.3 0.3">
      <geom type="sphere" size="0.008" rgba="1 0 0 0.4"
            contype="0" conaffinity="0"/>
    </body>

    <!-- Manipulation target: a small box the hand can pick up -->
    <body name="target_box" pos="0 0.3 0.025">
      <freejoint/>
      <geom type="box" size="0.025 0.025 0.025" rgba="0 0.8 0.2 1"
            mass="0.05" friction="1.5 0.05 0.001"/>
    </body>
  </worldbody>

  <!--
    Spring-damper weld (soft constraint):
      solref="0.01 1"       — 10 ms time constant; absorbs sudden hand jumps
      solimp="0.999 ..."    — near-rigid but won't explode on large discontinuities
  -->
  <equality>
    <weld body1="hand_proxy" body2="palm"
          solref="0.01 1" solimp="0.999 0.999 0.001"/>
  </equality>

</mujoco>
